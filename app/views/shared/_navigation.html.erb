<nav class="bg-white shadow-md border-b border-gray-200">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="flex justify-between items-center h-16">
      <!-- Left side: App name and optional greeting/page title -->
      <div class="flex items-center space-x-4">
        <%= link_to 'Kaizen Academy', root_path, class: 'text-xl font-bold text-gray-900 hover:text-blue-600' %>
        <% if local_assigns[:greeting] %>
          <span class="text-gray-600">|</span>
          <span class="text-gray-700"><%= greeting %></span>
        <% end %>
        <% if local_assigns[:page_title] %>
          <span class="text-gray-600">|</span>
          <span class="text-gray-700"><%= page_title %></span>
        <% end %>
      </div>
      
      <!-- Right side: Sign out -->
      <div class="flex items-center">
        <%= link_to 'Sign Out', destroy_user_session_path, 
            data: { turbo_method: :delete }, 
            class: 'bg-red-500 hover:bg-red-700 text-white font-medium py-2 px-4 rounded transition-colors duration-200' %>
      </div>
    </div>
  </div>
</nav> 